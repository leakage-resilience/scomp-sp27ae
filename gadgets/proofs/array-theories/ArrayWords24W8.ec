from Jasmin require import JWord JWord_array.

require import Array24 WArray24.

clone export ArrayWords as ArrayWords24W8  with op sizeW <- 1,
                                                op sizeA <- 24,
                                                theory Word <= W8,
                                                theory ArrayN <= Array24,
                                                theory WArrayN <= WArray24.
