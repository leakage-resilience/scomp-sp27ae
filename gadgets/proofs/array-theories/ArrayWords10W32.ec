from Jasmin require import JWord JWord_array.

require import Array10 WArray40.

clone export ArrayWords as ArrayWords10W32  with op sizeW <- 4,
                                                 op sizeA <- 10,
                                                 theory Word <= W32,
                                                 theory ArrayN <= Array10,
                                                 theory WArrayN <= WArray40.
