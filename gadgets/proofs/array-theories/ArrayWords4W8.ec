from Jasmin require import JWord JWord_array.

require import Array4 WArray4.

clone export ArrayWords as ArrayWords4W8  with op sizeW <- 1, op sizeA <- 4,
                                               theory Word <= W8,
                                               theory ArrayN <= Array4,
                                               theory WArrayN <= WArray4.
