from Jasmin require import JWord JWord_array.

require import Array3 WArray12.

clone export ArrayWords as ArrayWords3W32  with op sizeW <- 4, op sizeA <- 3,
                                                theory Word <= W32,
                                                theory ArrayN <= Array3,
                                                theory WArrayN <= WArray12.
