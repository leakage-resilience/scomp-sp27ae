from Jasmin require import JWord JWord_array.

require import Array40 WArray40.

clone export ArrayWords as ArrayWords40W8  with op sizeW <- 1,
                                                op sizeA <- 40,
                                                theory Word <= W8,
                                                theory ArrayN <= Array40,
                                                theory WArrayN <= WArray40.
